spring:  
  application:
    name: reservation-client
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
server:
  port: 8891
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8889/eureka
  