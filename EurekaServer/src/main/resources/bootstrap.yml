spring:  
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 8889
  
eureka:
  client:
    register-with-eureka: false
    fetchRegistry: false
    
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF